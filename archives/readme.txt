*.txt are raw files

alldata.csv is a table of all eligible authors and their data.
An author is eligible if he is affliated with a Montreal institution and has a valid email.

analyzed.csv is a table of every pair of authors and their common keywords.
upload this file to see a network graph with Google Fusion Tables

all other files are work in progress.

to run the python codes, you need to run C:\Program Files\MongoDB\Server\3.4\bin\mongod.exe (mongodb server) on a new command prompt!